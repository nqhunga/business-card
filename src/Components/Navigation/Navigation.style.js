import styled from 'styled-components';
import { Collapse, Navbar, NavbarToggler, NavbarBrand, Nav, NavItem} from 'reactstrap';
import { NavLink } from 'react-router-dom';


export const CollapseEx = styled(Collapse)`

`;

export const NavbarEx = styled(Navbar)`

`;

export const NavbarTogglerEx = styled(NavbarToggler)`

`;

export const NavbarBrandEx = styled(NavbarBrand)`

`;

export const NavEx = styled(Nav)`

`;

export const NavItemEx = styled(NavItem)`

`;


export const LinkEx = styled(NavLink)`

`;
