import styled from 'styled-components';
import {Table, Button, ButtonGroup, Input } from 'reactstrap';

export const TableEx = styled(Table)`

`;

export const ButtonEx = styled(Button)`

`;

export const ButtonGroupEx = styled(ButtonGroup)`

`;

export const InputEx = styled(Input)`

`;
